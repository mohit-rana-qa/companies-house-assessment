import { APIRequestContext, expect } from '@playwright/test'; export class Api { constructor(private request:APIRequestContext, private base=process.env.BASE_URL||'https://automationintesting.online'){} async health(){ const r=await this.request.get(`${this.base}/actuator/health`); expect(r.ok()).toBeTruthy(); return r.json(); } async rooms(){ const r=await this.request.get(`${this.base}/room`); return r.json(); }}